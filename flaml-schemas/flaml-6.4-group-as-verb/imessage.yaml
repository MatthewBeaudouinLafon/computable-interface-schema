# ios messages
- conversations:
    group foreach:
      - messages
      - people:
          groups: messages

- (gui) chat-view:
    /marks.text <>: conversations.active->messages
    /encoding.vstack <>:
      (linear) time:
        affects: messages, conversations
    /encoding.left-right-align <>: people

- (gui) convo-view:
    /marks <>: conversations
    /encoding.cluster <>:
      pin:
        groups: conversations
    /encoding.vstack <>: time

- (action) send-message:
    create: conversations.active->messages
    directions: time

- (action) select-conversation:
    update: conversations.active subsets> conversations
